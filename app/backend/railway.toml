[build]
# Removed deprecated NIXPACKS builder - enable Metal in Railway dashboard

[deploy]
startCommand = "yarn start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Volume configuration for SQLite database persistence
[[deploy.volumeMounts]]
mountPath = "/app/data"
