import { SocialProfile } from './social';

// Component renamed to SocialProfile but export name kept as FarcasterProfile for backward compatibility
export function FarcasterProfile() {
  return <SocialProfile />;
} 